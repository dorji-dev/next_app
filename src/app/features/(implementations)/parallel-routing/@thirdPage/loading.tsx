import ParallelRouteLoader from "@/components/features/parallel-routing/parallel-route-loader";

const Loading = () => {
  return <ParallelRouteLoader pageName="Third" />;
};

export default Loading;
